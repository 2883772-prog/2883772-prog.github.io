# wei 的项目公开介绍

静态介绍页，由 GitHub Pages 托管：

**https://2883772-prog.github.io/**

本地预览：用任意静态服务器打开仓库根目录，例如：

```bash
python -m http.server 8080
```

然后访问 `http://localhost:8080`。

说明：这是公开索引。多数原创仓库目前为私有，页面只介绍想法，不公开源码。
